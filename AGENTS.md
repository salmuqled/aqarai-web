## Cursor Cloud specific instructions

### Repository state

This repository ("AqarAi Web App") is currently empty — it contains only a `README.md`. No application code, frameworks, dependency manifests, or services exist yet.

### When code is added

Once the codebase has application code, update this section with:
- How to install dependencies (package manager, lockfile)
- How to run the dev server(s)
- How to run lint, tests, and builds
- Any external service dependencies (databases, caches, etc.)
- Non-obvious gotchas discovered during setup
