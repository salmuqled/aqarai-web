Upload your Instagram square template here as:

  template_main.png

Requirements:
- Exact size: 1080 x 1080 pixels
- PNG format
- Include logo; leave clear area for overlaid title/subtitle (see Cloud Function SVG positions)

Firebase Console → Storage → create folder "templates" → upload template_main.png
